public class ComputeArea {

  public static void main(String[] args) {
    double radius;
    double area;
    // step one in radius

    // step 2 compute area

    radius = 20;
    area = radius * radius * 3.14159;

    System.out.println("the area of circle is" + radius + "is" + area);

    // step 3, dispaly result
  }
}